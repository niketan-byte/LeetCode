class Solution {
public:
    int countSeniors(vector<string>& details) {
        int ans=0;
        for(int i=0;i<details.size();i++)
        {
            string str = details[i];
            int n = (str[11]-'0')*10+(str[12]-'0');
            if(n>60)
            {
                ans++;
            }
        }
        return ans;
    }
};
