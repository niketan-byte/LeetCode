class Solution {
public:
    vector<int> decode(vector<int>& encoded, int first) {
        vector<int>ans;
        int n=encoded.size();
        ans.push_back(first);
        for(int i=1;i<=n;i++)
        {
            ans.push_back(ans[i-1]^encoded[i-1]);
        }
    return ans;
    }
};
